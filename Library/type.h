#ifndef TYPE_H
#define TYPE_H

/** @brief Definition of `Type` that represent the data type with the programmer wants to use. 
			Modifing his definition, all functions that use this will works with that data type */
typedef int Type;

#endif // !TYPE_H